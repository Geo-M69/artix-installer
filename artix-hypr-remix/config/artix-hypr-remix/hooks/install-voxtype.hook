#!/usr/bin/env bash
set -euo pipefail

rm -f "$0"

if command -v notify-send >/dev/null 2>&1; then
  notify-send "Install Dictation" "Click to install voice dictation for artix-hypr-remix." -u critical
fi
