#!/usr/bin/env bash
set -euo pipefail

if pgrep -f "^gpu-screen-recorder" >/dev/null 2>&1; then
  printf '{"text":"󰻂","class":"active","tooltip":"Stop screen recording"}\n'
else
  printf '{"text":"","class":"hidden","tooltip":""}\n'
fi
