# Core AUR packages (phase 6 must fail if any of these fail).
# Keep this list minimal; optional UX extras should live in 9[1-9]-*.txt manifests.

# Example:
# package-name

# AUR helper bootstrap is handled in lib/aur.sh.
# paru